# What are Logs?
A log is a record of events that happen on a computer or server.

# Examples:
User login

Failed login

USB inserted

Program crashed

Malware detected

Service started

File deleted

SOC analysts inhi logs ko analyze karke attacks detect karte hain.

# Windows Logs
Windows logs Event Viewer me store hote hain.

# Open:
Win + R

eventvwr.msc

# Main log categories:

| Log | Description |
|------|-------------|
| Security | Records login, logout, authentication, user account changes, privilege usage, and security-related events. |
| System | Records Windows startup, shutdown, services, drivers, hardware, and operating system events. |
| Application | Records events generated by installed applications such as errors, warnings, and informational messages. |
| Setup | Records Windows installation, updates, and configuration-related events. |
| Forwarded Events | Stores events forwarded from remote Windows systems using Windows Event Forwarding (WEF). |

# Security Log (Most Important)
SOC ka sabse important log.

# Contains:
Successful login

Failed login

User creation

Password changes

Group changes

Account lockout

# Important Event IDs:

| Event ID | Meaning | Description |
|----------|---------|-------------|
| 4624 | Successful Login | A user successfully logged into the system. |
| 4625 | Failed Login | A login attempt failed due to incorrect credentials or other reasons. |
| 4634 | Logoff | A user logged off from the system. |
| 4648 | Explicit Credentials Used | A process used explicit credentials to authenticate. |
| 4672 | Admin Privileges Assigned | Special administrative privileges were assigned to a user. |
| 4688 | Process Created | A new process was created on the system. |
| 4689 | Process Ended | A process terminated or exited. |
| 4720 | User Created | A new user account was created. |
| 4726 | User Deleted | A user account was deleted. |
| 4728 | Added to Security Group | A user was added to a security-enabled global group. |
| 4732 | Added to Local Group | A user was added to a local security group. |
| 4740 | Account Locked | A user account was locked after failed login attempts or policy enforcement. |
| 4768 | Kerberos Ticket Requested | A Kerberos Authentication Ticket (TGT) was requested. |
| 4769 | Kerberos Service Ticket | A Kerberos Service Ticket (TGS) was requested to access a service. |

# System Log
Windows startup

Shutdown

Driver issues

Hardware errors

Service failures

# Example:
System started

Driver failed

Disk error

Unexpected shutdown

# Application Log
Browser crash

Office crash

Antivirus messages

Application warnings

# Example:
Chrome crashed

Python error

SQL Server started

# Windows Defender Logs
Malware detected

Virus removed

Threat quarantined

# PowerShell Logs
SOC analysts monitor PowerShell because attackers often use it.

# Look for:
Encoded commands

Download commands

Execution Policy changes

# Example:
powershell.exe

Invoke-WebRequest

IEX

DownloadString

# Sysmon Logs
Sysmon gives detailed security events.
# Examples:
Process creation

Network connections

DLL loading

Registry changes

File creation

SOC teams almost always collect Sysmon logs.

# Linux Logs
Most Linux logs are stored in:

/var/log/

# Common logs:

| Log File | Description |
|----------|-------------|
| `/var/log/auth.log` | Stores authentication events, SSH login attempts, sudo usage, and user authentication logs. |
| `/var/log/syslog` | Stores general system messages, services, daemons, and kernel-related events. |
| `/var/log/messages` | Stores general system messages on some Linux distributions (e.g., RHEL, CentOS). |
| `/var/log/kern.log` | Stores Linux kernel messages, driver events, and hardware-related logs. |
| `/var/log/boot.log` | Stores system boot process and startup messages. |
| `/var/log/dmesg` | Stores kernel ring buffer messages, including hardware detection and device initialization. |
| `/var/log/apache2/access.log` | Records all incoming HTTP requests handled by the Apache web server. |
| `/var/log/apache2/error.log` | Records Apache server errors, warnings, and debugging information. |
| `/var/log/nginx/access.log` | Records all incoming HTTP requests handled by the Nginx web server. |
| `/var/log/nginx/error.log` | Records Nginx server errors, warnings, and debugging information. |

# auth.log (Most Important Linux Log)
SSH login

sudo commands

User authentication

Failed login

# Example:
Accepted password

Failed password

sudo

session opened

# syslog
Services

System messages

Network

Daemons

# Kernel Log
Hardware errors

Driver loading

Kernel messages



#  What is Splunk?
Splunk is a SIEM and log management platform used to collect, search, analyze, monitor, and visualize machine-generated data (logs) from different systems.

# What is a SIEM?
SIEM stands for Security Information and Event Management.

It collects logs from different devices, detects threats, correlates events, and helps security analysts investigate incidents.

# What is an Index?
An Index is a storage location where Splunk saves and organizes logs.

# Example:
windows

linux

firewall

antivirus

Think of it as a database for logs.

# What is a Host?
A Host is the machine or device that generated the log.

# Examples:
Windows PC

Linux Server

Firewall

Router

# Example:

Host = SERVER01
#  What is a Source?

A Source tells Splunk where the data came from.

# Example:

C:\Windows\System32\winevt\Logs\Security.evtx

or

/var/log/auth.log
#  What is a Sourcetype?
A Sourcetype tells Splunk what type of data it is so Splunk knows how to parse it.

# Examples:
WinEventLog

syslog

apache

nginx

csv
#  What is a Forwarder?
A Forwarder is a lightweight Splunk agent installed on a machine that collects logs and sends them to the Splunk Indexer.

# Example:

Windows PC
      ↓
Universal Forwarder
      ↓
Indexer
#  What is an Indexer?
The Indexer receives logs from forwarders, indexes them, stores them, and makes them searchable.

Think of it as the storage and processing engine.

# What is a Search Head?
The Search Head is where users search data, run queries, create dashboards, and investigate logs.

It sends search requests to Indexers and displays the results.

# What is the Search Bar used for?
The Search Bar is used to write SPL (Search Processing Language) queries to search and analyze logs.

# Example:

index=main
# What is the Time Picker used for?
The Time Picker filters logs based on time.

# Examples:
Last 15 minutes

Last 24 hours

Last 7 days

Custom time range
# What are Events?
An Event is a single log entry stored in Splunk.

# Example:
User John logged in successfully.

Each log line is one event.

# What is the Statistics Tab?
The Statistics tab displays search results in a table format.

It is useful for:
Counts

Grouping

Filtering

Aggregations

# Example:

|User |	Count |
|_____|________|
|John |	 15    |
|Admin|	30     |

#  What is the Visualization Tab?
The Visualization tab converts search results into charts and graphs.

# Examples:
Bar Chart

Pie Chart

Line Chart

Area Chart

Single Value

Maps

It helps create dashboards for easier analysis.

## 📘 Splunk 

| Concept | Definition |
|---------|------------|
| **Splunk** | Log management & SIEM platform |
| **SIEM** | Security Information and Event Management |
| **Index** | Stores logs |
| **Host** | Machine generating logs |
| **Source** | Original location of the log file |
| **Sourcetype** | Type or format of the log data |
| **Forwarder** | Sends logs from endpoints to Splunk |
| **Indexer** | Receives, stores, and indexes logs |
| **Search Head** | Runs searches and displays results |
| **Search Bar** | Used to write SPL (Search Processing Language) queries |
| **Time Picker** | Filters logs based on a selected time range |
| **Event** | A single log entry in Splunk |
| **Statistics** | Displays search results in table format |
| **Visualization** | Converts search results into charts and dashboards |