# ✅ What I Did
Learned the stats command.

Counted events.

Grouped events by host.

Grouped events by source.

Grouped events by sourcetype.

Used dc() to count unique values.

Used values() to list unique field values.

Learned aggregation functions like avg(), max(), min(), and sum().

Practiced summarizing large datasets.

# 📚 What I Learned
stats summarizes search results.

count counts matching events.

by groups results based on one or more fields.

dc() counts distinct values.

values() returns unique values.

avg(), max(), min(), and sum() perform calculations on numeric fields.

stats is one of the most commonly used commands in SOC investigations.