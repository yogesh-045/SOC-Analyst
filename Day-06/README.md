# ✅ What I Did
Learned the eval command.

Created custom fields.

Filtered events using where.

Renamed fields using rename.

Removed duplicate events using dedup.

Combined multiple commands in a single SPL query.

# 📚 What I Learned
eval creates or modifies fields.

where filters results after search processing.

rename changes field names for readability.

dedup removes duplicate events.

These commands are frequently used in SOC investigations and dashboards.